// Extends Vitest's `expect` with jest-dom matchers (toBeInTheDocument, etc.)
// used by component tests. Registered via vitest.config.ts `test.setupFiles`.
import "@testing-library/jest-dom/vitest";
